import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette - Deep teal inspired by Islamic architecture
  static const Color primary = Color(0xFF00796B);
  static const Color primaryLight = Color(0xFF48A999);
  static const Color primaryDark = Color(0xFF004C40);

  // Secondary palette - Warm gold accents
  static const Color secondary = Color(0xFFC6993E);
  static const Color secondaryLight = Color(0xFFFFD54F);
  static const Color secondaryDark = Color(0xFF8D6E2E);

  // Surface colors
  static const Color surface = Color(0xFFF5F5F5);
  static const Color surfaceCard = Colors.white;
  static const Color background = Color(0xFFFAFAFA);

  // Semantic colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA726);
  static const Color info = Color(0xFF29B6F6);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = Colors.white;
}
